(function(global, factory) {
    if (typeof module !== "undefined" && module.exports) {
        // Node.js/CommonJS
        module.exports = factory();
    } else {
        // Browser
        global.vulture = factory();
    }
})(this, function() {
    // Your entire vulture.js code here
    let form, forms = [], multiConnect = false;

    const vulture = {
        connect: (r) => { /* your function code */ },
        multiConnect: (r) => { /* your function code */ },
        talon: ({ strict, augment, render_error = true }) => { /* your function code */ },
        talonAll: (r, { strict, augment, render_error = true }) => { /* your function code */ },
        formatter: (r) => { let e = {}; r.forEach((r) => { e[r.name] = r.value }); return e; }
    };

    return vulture;
});
